# User QA

Record user decisions and open product questions here.

Files with `## Status` set to `Answered` are accepted decisions even when their
historical filename begins with `pending-`. `pending-example.md` and
`qa-example.md` are templates and are not project decisions.
